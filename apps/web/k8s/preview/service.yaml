apiVersion: v1
kind: Service
metadata:
  name: tanstack-observatory
  namespace: pr-${PR_NUMBER}
  labels:
    app.kubernetes.io/name: tanstack-observatory
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tanstack-observatory
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
